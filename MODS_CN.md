# ReadAny-Mods 自用魔改版说明

> 本仓库是 **ReadAny 的个人自用魔改版**(fork 自 [codedogQBY/ReadAny](https://github.com/codedogQBY/ReadAny),基于上游 2026-08-16 版本)。
> 功能与官方保持独立,`main` 分支官方版,本说明描述的是 `local-mods` 分支上的个人改动。
>> 感谢原作者带来的优秀开源项目。本人非专业开发者。

> **⚠️ 改动全基于 AI，兼容性未测试。**

## 已知限制

- **同步功能为 Beta** — 提供 **WebDAV**（坚果云 / Nextcloud 等）、**S3 兼容存储**、**局域网快传**三种方式，其中 WebDAV 是主要适配与实测路径；S3 与局域网快传代码可用但未充分验证。
- **在线书源为实验性** — 「本机书源服务端」与 Z-Library / LibGen 驱动挂在开发者模式开关后面，上游站点随时可能变更导致失效。

## 改动总览(按平台)

### Android(安卓 App)

**启动与冷启动**
- **冷启动直达上次阅读的书** — App 被杀前停在阅读页，下次启动直接进阅读器，不闪书库
- **直达后按返回键回书库不退出 App**
- **书库数据后台预热** — 首次返回书库不再等查询
- **首屏封面预载** — 启动后把首屏 9 本封面解码进缓存，第一帧就有封面
- **WebView 内核预热** — 启动即建 1×1 隐藏 WebView，点书进阅读页更快
- **reader.html 提前下载** — 2MB 阅读器引擎包启动时后台预下载
- **TTS 播放引擎延后初始化** — 推迟到启动 500ms 后，不占冷启动首帧
- **章节提取器常驻** — 返回书库不再重建 WebView 重跑引擎，消除「首次返回卡 ~2s」
- **AI 书库检索会话常驻** — tab 切换不再导致 AI 查书内容超时

**阅读器内核**
- **阅读器顶栏改版** — 从「章节名+页码」改为只显示书名、加粗 16 号、贴屏幕顶
- **底部信息条三段式重构** — 照静读天下：电量+时间 / 章节页码 / 全书进度%
- **正文区高度恒定，翻页不再抖动** — 面板开合不再触发 foliate 重排
- **沉浸式全屏：面板显隐联动状态栏+三键** — 合入同一个系统动画，消除二段式卡顿
- **退出阅读器恢复系统栏**
- **控制栏动画 180ms → 250ms**
- **浮动工具条与底部控制栏预判三键高度** — 不再被三键弹出「顶一下」
- **单击侧边翻页开关**
- **音量键翻页**
- **长按查词触发方式可选** — 自动弹词 / 松手弹词
- **长按直接查词（双定时器手势）** — 长按 400ms 选中+短振，再 400ms 弹词，与拖动选词共存
- **划词菜单精简+放大** — 只剩 发音/复制/AI 对话/翻译，尺寸放大 1.25 倍
- **阅读设置面板适配 edge-to-edge**
- **阅读设置面板空隙贴合修复** — 紧贴状态栏、高度自适应可滚动
- **WebView 自愈** — 转场动画导致 JS 不初始化时 8 秒后重建（最多 2 次）
- **首渲染瘦身** — 工具栏/信息条延迟 400ms 挂载
- **打开书后台接力其他设备进度** — 有远端进度则静默同步并跳转（60 秒节流）
- **恢复位置改直读进度账本**
- **退书只上传不重写** — 消除进度回退/抖动
- **AI 引用闪烁高亮改深蓝**
- **悬浮 AI 按钮长按 = 为该书写开新会话**
- **章节翻译「清空」语义修正** — 真正清缓存，否则自动恢复会把它注回
- **整章翻译强制选择模型**
- **AI 上下文带上完整目录与章节名**
- **三键区域背景与图标配色** — 自绘主题色底条，图标随深浅主题转白/黑
- **划词选区不再漂移/跳页** — 整页漂移守卫 + selectionchange 新鲜度判定
- **阅读设置三键图标随 App 主题**

**词典与查词**
- **长按查词可选「词典接口」** — 9 项固定接口表（欧路/深蓝/MDict/ColorDict 联动、有道、金山词霸、WordWeb、牛津、韦氏、百度、海词、自定义 URL）
- **本地离线词典下载中心（ECDICT）** — 精选 65 万词条 ~74MB / 全量 340 万 ~291MB，魔塔源、断点续传
- **内置查词结果弹窗** — 居中卡片显示词+释义
- **查词自动发音 + 喇叭重读**
- **本地词典未命中可「AI 兜底」** — 关闭时明确提示而非静默失败
- **TTS 音频 LRU 缓存** — 重复朗读同一段秒出声（5 条上限，全云引擎共用）
- **查词走独立采样参数** — temperature 0.2 / topP 0.8 / repetitionPenalty 1.1
- **查词结果与普通翻译缓存隔离** — 缓存键带提示词指纹
- **未装词典/未配置自定义 URL 有明确中文提示**

**翻译**
- **取词翻译/查词按翻译引擎分流** — 外部词典接口 vs 内置引擎
- **划词即翻译**
- **翻译设置整体对齐桌面版** — 引擎下拉 + 取词/长按两个独立子卡
- **取词翻译与长按查词各自独立选模型**，不再回落全局

**在线书源（OPDS）**
- **在线书源整体上线** — 可添加/管理自己的 OPDS 目录地址
- **书源管理列表** — 增/删/改/进入目录，删除连带清凭据
- **书源添加/编辑表单** — 名称、地址、Basic 认证、允许不安全连接、测试连接
- **在线目录浏览** — 面包屑导航 + 子目录 + rel=next 分页
- **在线目录搜索** — OpenSearch 模板与内联模板两种
- **分面筛选** — 排序/语言/格式 chips，长清单折进「其他…」弹层
- **书籍详情页** — 大封面+元数据+简介
- **相似书籍 / 评论弹层**
- **多格式下载选择** — 弹层列出（大小 + 推荐标）
- **下载入库带进度 + 坏文件校验** — 校验 epub=PK / pdf=%PDF，HTML 错误页拒绝入库
- **在线书源导入 = 纯本地** — 标记为不上传云端
- **导入来源菜单合并** — 原「OPDS 在线目录」与「书源管理」两行并一行
- **本机书源服务端（开发者，实验性）** — 本机 127.0.0.1 起 OPDS 服务（LibGen + Z-Library）
- **Z-Library 在线书源配置（开发者）** — 账号/密码/固定镜像 + 测试连接

**书库与云书库**
- **书库顶部可切换到「云书库」** — 标题旁云图标一键切换
- **云书库视图** — 列出云端书目（书名/作者/大小/进度），逐本下载入库
- **书籍「绑定」角标** — 已上传云端的书显示黄色标
- **单书菜单「上传云端」**
- **多选批量上传云端**
- **删除书籍时可一并删云端副本**（默认不勾）
- **书库封面解码优化** — 按目标尺寸下采样 + 淡入 150ms
- **返回书库不再卡顿** — 不重拉数据库、不重载同步配置、不重建提取器
- **书库按进度排序走唯一账本**
- **清理重复书籍** — 按内容哈希删重复/云端幽灵记录，完成后报告数量

**同步界面**
- **同步设置默认收起高级区**
- **移除「强制上传 / 强制下载」按钮**
- **同步远端目录默认名 readany → RA_dev**
- **云书库下载失败提示更具体** — 区分「远端没有文件」与一般失败
- **同步日志悬浮横幅（开发者）** — 实时显示同步走到哪一步

**AI 聊天**
- **双层可拖拽输入框** — 向上拖/甩拉出下层功能面板
- **AI 模式滑动开关** — 标准/Lite/K-O 三段滑块，弹簧吸附
- **深度思考与防剧透移入下层面板** — 防剧透按场景分别记忆
- **工具开关菜单** — 只列当前模式可选的工具
- **切换模式/深思考/防剧透时顶部提示 3 秒**
- **思考卡流完自动折叠** — 手动展开过的不会被重折叠
- **思考卡显示「思考了 N 秒」**
- **三层 Token 账本** — 思考卡/工具卡显示本段消耗，每条回答页脚显示 `+546/9,764sum`
- **上滑看历史不再被强拉回底部**
- **会话上下文卡片** — 首条系统消息渲染成卡片而非气泡
- **引文 chip 可点击跳回原文并高亮**
- **划词进 AI 对话带精确 CFI 定位**
- **发送后引文 chip 立即消失**
- **清空全部会话** — 二次确认
- **模型选择器只列当前端点 + 搜索框**
- **会话切换即时生效** — 消除原 ~1 秒延迟
- **长回复流式渲染分块** — 只有最后一块重算布局
- **切入会话卡顿治理** — 先纯文本渲染，再每 100ms 错峰富化为 Markdown
- **正文实时流（开发者开关）** — 边收边吐 vs 整轮缓冲
- **书内聊天时三键常驻**，退出后按原状态恢复
- **输入栏键盘遮挡修复** — 原生 ime insets 读数，输入卡悬浮不参与布局
- **消息工具标签同步** — 移除已删工具的标签映射

**统计**
- **微信式双层下拉统计面板** — 书库页顶部下拉，第一页整体上移、面板跟出
- **统计卡内容调整** — 改总时长/当日时长/已读本数/连续天数，移除字数与阅读速度
- **热力图随统计面板** — 16 周活动热力图，点格子看当日时长
- **「我的」页不再显示统计卡与热力图**
- **统计详情页瘦身** — 移除阅读字数/速度/场次
- **统计详情页支持左滑返回**
- **统计内书籍进度改走唯一账本**

**设置**
- **AI 设置端点列表卡片化**，AI 助手区独立成块且默认收起
- **AI 设置提供商改下拉选择**
- **AI 设置长说明收进问号弹窗**
- **模型列表折叠 + 搜索** — 超 25 个默认收起
- **「获取模型」不再无限转圈** — 加传输预算超时
- **小输入框文本完整显示** — 关闭 Android 默认上下内边距
- **书详情语言字段归一化** — zh-CN/ZH-HANS 等变体正确匹配

**导航与开发者模式**
- **底部 tab 栏改为自绘** — 纯点击切换，删除左右滑动切换
- **点 tab 立即收起下拉面板**
- **开发者模式隐藏入口** — 长按「我的」tab 3 秒
- **长按触发后本次点击被吞掉**
- **开发者模式页内容** — 启动动画/同步横幅/实时流/本机书源/ZL 配置/顶栏背景/底栏背景

**底层体验（打开书与渲染）**
- **打开书不再偶发「内容加载失败」** — Lighttpd 失败后会话级降级为 JS TCP 服务器
- **书内资源请求偶发 502/空响应修复** — HTTP/1.1 keep-alive + 显式 Content-Length
- **书内 AI 检索不再超时/报错** — 常驻单例 WebView 检索会话，按书分 session
- **书库封面首帧不闪** — 封面 URI 内存缓存
- **AI 输入框可「拖动查看文本」而不弹键盘** — 原生 ReactEditText patch
- **阅读器状态栏+三键同步显隐** — 自研 ReaderSystemBars 原生模块
- **键盘遮挡输入栏精确修复** — 自研 ReaderKeyboardInsets 模块读 ime insets
- **应用名 RA dev / 包名 com.readany.app.dev** — debug 与 release 并存不覆盖
- **启动图/图标底色/状态栏统一深蓝 #05042B，竖屏锁定**

### Windows(桌面端)

**查词与翻译**
- **长按取词查词** — 正文按住鼠标约 0.3 秒自动高亮并弹词典
- **划词即翻译** — 选中松手立刻弹翻译结果，可在阅读设置关掉
- **查词弹窗喇叭重读**
- **查词自动发音** — AI 查词与 ECDICT 两方案都生效
- **ECDICT 本地离线词典** — 约 340 万词条，完全离线毫秒级
- **查词方案二选一** — AI 查词 / 本地词库
- **本地词库查不到时 AI 兜底**（开关式）
- **查词提示词自定义** — 支持 `{word}` 占位符 + 一键恢复默认
- **翻译弹窗刷新按钮** — 清该词缓存并重请求
- **翻译弹窗可拖拽拉伸** — 尺寸按「取词翻译/长按查词」分别记忆
- **翻译弹窗智能定位** — 优先贴选区左右，空间不够才翻转，不遮挡被查词
- **翻译弹窗底部复制行常驻**
- **弹窗显示具体模型名**（原显示端点名）
- **查词弹窗显示当前查词方式** — 本地词库 / AI 查词
- **取词翻译 / 长按翻译 两套独立模型** — 各自选端点和模型，互不兜底
- **翻译请求去重** — StrictMode 双跑导致同请求发两次

**阅读体验**
- **平滑翻页动画开关** — 同章翻页 300ms 过渡
- **单击任意位置切换工具栏** — 取消原「左/右 1/3 翻页」分区
- **右键取消划词**
- **屏蔽正文区原生右键菜单**（全应用范围）
- **点同一个颜色取消高亮**
- **颜色栏内置垃圾桶** — 独立删除按钮
- **高亮颜色栏展开状态被记忆**
- **标题栏显隐开关** — 可隐藏顶部窗口标签栏
- **阅读区不再悬浮显示章节标题**
- **阅读设置面板贴合顶栏** — 0 间隙、高度自适应可滚动
- **关闭自动断词连字符** — `hyphens: none`
- **章节翻译需先选模型**
- **工具栏改为窗口拖拽区**
- **阅读器内容区随标签栏平滑上移**（300ms）

**阅读进度账本**
- **阅读进度改为账本记账** — 开书 1.5 秒稳定窗口、翻页 0.1% 抖动过滤
- **每翻 5 页自动后台同步**
- **退出阅读器强制落盘**
- **卡片/列表/详情页进度改读账本**
- **按进度排序走账本**
- **打开书前先恢复账本位置**

**AI 助手与聊天**
- **三模式滑块** — 标准 / Lite / K-O，每档有悬停说明
- **工具开关菜单** — Lite/K-O 可逐项开关，标准模式显示禁用态
- **双层滑动抽屉输入框** — 手柄拖拽拉出/收起功能面板，展开状态记忆
- **Alt+O 快捷键** — 快速展开/收起功能面板
- **防剧透按上下文独立记忆**
- **模型选择器只列当前端点 + 搜索框**
- **思考卡流完自动折叠 + 显示「思考了 N 秒」**
- **思考卡手动展开会被记住**
- **每步 token 计量** — 思考卡/工具卡显示 `1,234tk`
- **每轮 + 会话累计 token** — 右下角显示 `1,234sum / 5,678sum`
- **Mermaid 图表渲染**
- **新提问必定滚到底** — 不再因为之前上翻被卡住
- **流式渲染长列表性能优化** — 消息转换不再随 160ms 分片重复执行
- **正文实时流开关（开发者）**
- **对话引用附件（引文 chip）** — 可单个移除、超长截断

**设置面板**
- **AI 设置折叠化** — 端点选择与参数区可整块收起
- **模型列表折叠** — 超 25 个默认收起
- **模型搜索限流** — 最多渲染 100 条
- **点选模型联动三处** — 同时设为默认端点/全局模型/测试模型
- **测试模型改胶囊按钮**
- **可搜索的模型下拉框** — 所有模型选择器统一
- **新增「开发者选项」设置页**

**开发者模式（隐藏功能）**
- **连点版本号 5 次开启开发者模式**（1.5 秒内）
- **开发者选项面板** — 聚合本机书源/ZL 配置/同步横幅/实时流/DevTools
- **本机书源开关** — 进程内直连，不监听端口、不受系统代理/VPN 干扰
- **Z-Library 账号与镜像配置 + 测试连接**
- **同步日志横幅** — 顶部常驻显示最近 8 条步骤
- **打开界面检查器** — WebView DevTools（或 F12 / Ctrl+Shift+I）
- **退出开发者模式** — 连带关掉本机书源/同步横幅/实时流

**书库与云同步**
- **云端书库对话框** — 列出云端所有书籍
- **云端书籍下载导入** — 自动入库，已存在则提示
- **云端删除（可选连本地一起删）**
- **云端列表信息展示** — 大小、含封面、跨端进度百分比
- **云端书库刷新按钮**
- **上传至云端（绑定同步）** — 已绑定显示「更新云端文件」
- **卡片「绑定」徽标**
- **删除本地书时可选同时删云端**
- **清理重复书籍**
- **同步远端目录默认值改名 RA_dev**
- **云朵图标换成安卓同款**
- **书架顶部新增云书库入口按钮**

**OPDS 在线书源**
- **导入菜单新增 OPDS 入口**
- **书源管理对话框**
- **添加/编辑书源** — 地址、Basic 认证、允许不安全连接
- **书源测试连接** — 名称留空时自动用目录标题填充
- **删除书源二次确认**（提示凭据一并清除）
- **OPDS 目录浏览器** — 大尺寸窗口（94vw、最高 1000px）
- **面包屑导航 + 返回按钮**
- **网格 / 列表双视图切换**
- **OPDS 搜索** — 回车提交、一键清空、独立搜索按钮
- **分面筛选条** — 排序/语言/格式三组可点胶囊
- **分面「其他…」弹层** — 191 种语言收进弹窗，可搜索过滤
- **OPDS 书籍详情弹窗** — 高清封面 + 完整简介 + 全部格式
- **详情弹窗自动补全信息** — 列表没给格式/简介时请求详情接口
- **更多相似书籍**
- **读者评论** — 用户名、日期、会员星标
- **「已入库」标记** — 绿色标（网格角标 / 列表胶囊）
- **下载进度可视化** — 书封覆盖层 + 底部状态条
- **悬浮快捷下载按钮**（网格视图）
- **多格式下载菜单**（列表视图）
- **下载内容校验** — PK / %PDF 文件头
- **加载更多分页** — 追加而非替换
- **加载中横幅而非白屏**
- **失败/空态「重试」重放而非退回上一级**
- **请求竞态防护** — 过期响应丢弃
- **键盘返回** — Backspace / Alt+←
- **拟物书封** — 带书脊反光与纸质渐变兜底
- **OPDS 导入的书标记为「纯本地」**

**其他界面**
- **禁用原生右键菜单**（WebView2）
- **引导页翻译引擎默认值更稳健**
- **远端书下载失败文案更准确**
- **dev 端口 1420→3420、HMR 1421→3421** — 规避 Windows WinNAT 保留端口冲突

### 共性(双平台共享)

**AI 三模式体系**
- **聊天模式三选一** — 标准 / 轻量(Lite) / 纯知识(K-O)
- **纯知识模式为默认模式** — 新会话默认走它，回答快、不检索原文
- **纯知识模式的角色设定** — 只答作者背景/出版信息/体裁风格等，不得声称核对过原文
- **纯知识模式问「这章讲了什么」直接给模式切换指引**，不再空转思维链
- **纯知识模式支持防剧透**
- **轻量模式支持防剧透** — 且禁止用 fallback 工具取当前位置之后的章节
- **轻量模式为快速直聊变体** — 去掉路由、工具目录、工作流
- **轻量/纯知识跳过「记忆压缩」这一轮额外 LLM 调用** — 首 token 更快
- **轻量模式工具链递归上限 24 → 16**

**AI 工具开关**
- **按模式提供可选工具清单** — 轻量 15 项、纯知识 9 项，默认全关
- **轻量模式 6 项常开工具不可关闭**
- **轻量模式强制屏蔽重分析与引用工具**
- **纯知识模式强制屏蔽检索/引用/分析/书库写工具**
- **纯知识模式可开关的基础读工具**（高亮/批注/书库列表/统计/思维导图/技能）
- **开启「技能」后该模式技能工具自动注册**
- **AI 会告知用户某工具处于关闭状态并指路怎么开**

**AI 上下文注入**
- **首轮注入静态书籍信息并持久化** — 作为会话第一条 system 消息存库
- **AI 能看到书籍简介与主题词**
- **每轮注入实时阅读位置** — 章节标题+索引、进度百分比
- **每轮注入当前选中文本**（截断 2000 字）
- **当前章节标题兜底** — 从持久化 TOC 按 index 回填
- **引文上下文预取** — 按 CFI 预取引文前后原文（最多 6000 字）
- **引文位置信息不再混进可见引用行**
- **阅读器 TOC 被快照保存**

**AI 检索质量**
- **书籍语言推断** — `meta.language` 缺失时从章节标题+正文采样推断
- **跨语言提问的检索词引导** — 只用书中语言的原词
- **语言未知时的兜底引导**
- **检索工具描述内嵌书籍语言提示**
- **`ragSearch` 改为返回「位置清单」而非整段正文**
- **`ragSearch` 默认返回条数 5 → 20，上限 50**
- **`ragSearch` 摘录预算按真实字符计算**（旧代码对中文低估约 4 倍）
- **`ragSearch` 优先用命中片段而非块首**
- **`ragSearch` 非法 mode 不再崩溃**
- **`ragContext` 支持锚点读取 (anchorCfi)** — 可直达长章节中段/尾部
- **`ragContext` 报告覆盖情况** — 模型知道自己是否读全
- **`ragContext` range 限制在 1-10**
- **模型被告知「先 ragSearch 定位、再 ragContext 读」两步法**
- **章节引用解析支持「Chapter 9」/「CHAPTER 09」前缀**
- **章节引用解析支持「4. Launch」/「4、标题」前缀**
- **`ragToc` 优先从阅读器会话秒取真实目录**
- **`resolveChapterReference` 优先用真实目录标题**
- **目录条目带 `href`，模型按 href 直取章节**
- **`fallbackChapterContext` 新增 `href` 参数**
- **`fallbackToc` 查询过滤放宽**（零填充数字互配）
- **`fallbackToc` 默认引导「不带 query 拉全表自己挑」**
- **fallback 三工具在安卓改走常驻阅读会话**
- **非向量化书的检索结果带精确 CFI**
- **`fallbackSearch` 查询词引导改为「2-5 个紧凑词」**
- **`fallbackSearch`/`fallbackChapterContext` 空查询直接报错**
- **书未在阅读器打开时明确告知模型「不可重试」**
- **非向量化书上的引用直接信任精确 CFI**
- **移动端引用回填改为单章读取**（5 秒上限，删掉 45s 卡死路径）

**AI 分析工具不再谎报覆盖**
- **`summarize`（整本）预算摊到每一章** — 旧实现 100 章只读前 27 章开头却报真实章节数
- **`summarize`（整本）返回明确覆盖块**
- **分析工具采样不完整时明确标注 PARTIAL**
- **`summarize`（单章）也带覆盖块与 truncated 标记**
- **`extractEntities` 带覆盖块**
- **`analyzeArguments` 带覆盖块与截断提示**
- **`findQuotes` 带覆盖块与截断提示**
- **`compareSections` 两章各自带覆盖块**
- **`summarize`（整本）不再重复遍历** — O(章节数×块数) 降为一次分组

**Token 三层账本**
- **思维卡显示思维链自身的 token 数**
- **工具卡显示该轮调用的 token 消耗**
- **每条回答页脚显示「本轮 / 会话累计」** — `+546/9,764sum`
- **会话累计是「这个对话烧了多少 token」的累加**，非「对话有多大」
- **流式过程中页脚累计值实时增长**
- **中止或报错的回答也记下已花费的 token**
- **token 徽章与轮次总量重载后保留**
- **思维卡显示「思考了 N 秒」**
- **分类账为「每次 LLM 调用一条」** — 避免逐部件相加重复计数
- **直连路径（无工具）也上报 token 用量**
- **中文按 1 字 1 token 计数**

**AI 超时与中断**
- **流式首事件看门狗 90 秒**
- **流中间隔看门狗 120 秒 / 工具执行期间 150 秒**
- **超时提示文案本地化**
- **响应头超时 120 秒（传输层兜底）**
- **关闭 LangChain 的 6 次自动重试** — 一次超时从约 8 分钟缩到看门狗时长
- **记忆压缩 25 秒预算** — 原本不受任何看门狗覆盖
- **设置页「测试连接/获取模型」15 秒预算**
- **点「停止」真正中断 HTTP 请求**（不再继续计费）
- **中止路径收口** — 避免 `isStreaming` 卡在 true 阻塞后续发送

**AI 对话正确性修复**
- **不再重复发送当前用户消息**
- **完成态的回答消息现在持久化 `parts`**
- **DeepSeek 工具调用不再报 400**
- **滑动窗口保留置顶的系统消息**
- **记忆压缩窗口边界修正**

**AI 问题路由**
- **「当前/这一章/本章」类提问优先于裸章节号**
- **未命中任何线索的书内提问保留检索工具**
- **「是否像书内容问题」只影响引导语、不影响工具集**
- **`getSurroundingContext` 合并原 `getCurrentChapter` 职责**
- **`getSurroundingContext` 锚点是「选中文本优先，否则阅读位置」**
- **`getSurroundingContext` 单独返回选中文本**

**AI 书库与进度**
- **`listBooks` 阅读状态筛选改用唯一进度账本**
- **`getReadingStats` 在读/读完统计同样改用唯一账本**
- **防剧透基于真实进度**

**同步语义 v2**
- **书行不再跨设备同步** — `books` 表整个退出同步快照
- **重复书/幽灵书/吃书从结构上根除**
- **AI 对话不再同步** — 聊天记录只在本机
- **书库分组与标签不再同步**
- **高亮/笔记/书签/阅读会话继续同步**，改为按「书籍内容哈希」跨设备认亲
- **跨设备记录自动挂到本地同内容的书上**
- **本地没有这本书时远端标注自动跳过** — 不再刷屏
- **同书同位置的重复标注去重**
- **旧格式（无 book_hash）远端记录前置跳过**
- **每设备「已应用」指针** — 重复同步显著提速
- **阅读进度跨设备同步** — 新增 `reading_progress` 表按内容哈希进快照
- **进度冲突改按时间戳裁决（LWW）**
- **同步完成后先重载进度再通知 UI**
- **打开书时先做 stat 探测** — 远端无变化则跳过完整同步
- **单设备用户打开书不再触发拉同步**
- **无变更时快照保活窗口 5 分钟 → 30 分钟**
- **后台快速上传** — 滑走 App 时只推自己快照（约 300ms）
- **同步进度新增设备级文案**
- **同步日志横幅（开发者选项）**

**云书库与文件传输**
- **云书库入口** — 与本地书库并列
- **云端书目列表** — 书名/大小/是否带封面/本地是否已导入
- **云端书手动下载**
- **本地书「上传云端」（绑定）** — 同内容已在云端则跳过
- **云端书删除**
- **删除云端后本地绑定角标立即消失**
- **书库卡片「绑定」角标**
- **同步完成后自动刷新绑定角标缓存**
- **删除本地书时可勾选「同时删除云端副本」**
- **云书库/书库显示阅读进度**
- **默认远端根目录改名 `readany` → `RA_dev`**
- **已配置旧根 readany 的用户继续读写原目录**（双根兼容）
- **WebDAV 目录探活缓存 30 分钟**
- **不再创建 legacy file/cover 目录**
- **远端书文件改为平铺单文件布局** — `{书名}-{内容SHA256}.{ext}`
- **本地删书不再删云端文件** — 云端定位为「备份+中转」
- **封面必须伴随书文件才上传** — 修掉云端凭空多出只有 jpg 的空壳书
- **纯本地书（在线书源导入）永不上传云端**
- **文件同步进度文案改中文**
- **单书下载走 v2 路径**
- **旧 `{title}-{uuid}` 目录与 legacy 布局不再读写**

**存量数据清理**
- **清除云端幽灵记录** — 一键删除「云端不存在、本地无文件、且无任何标注」的书行
- **存量重复书按内容 hash 去重**
- **保守安全边界，不误删** — 有本地文件/带标注的一律保留并写进报告

**进度账本**
- **新增唯一进度账本 `reading_progress`** — 以内容哈希为主键
- **升级时自动回填** — 老库启动时把 `books.current_cfi/progress` 灌进新账本
- **新增进度读模型 store** — 书库角标/详情/排序统一从这里读

**统计**
- **统计页整体提速** — 逐本 SELECT 的 N+1（约 400ms）改为 3 条聚合 SQL
- **每日阅读统计下推到 SQL** — 拉 365 天只有 ≤365 行跨桥
- **统计报告（ETA/已读计数）改读新账本**
- **分段统计书目进度改读新账本**
- **阅读会话保存不再因同 id 冲突失败**（UPSERT）
- **会话写入并行取元数据** — 每次保存少约百毫秒
- **章内分块顺序修复** — `…-10` 不再排在 `…-2` 前面

**在线书源核心（OPDS）**
- **OPDS 在线书源增删改** — 最多 50 条
- **书源密码独立加密存储** — 编辑时留空=保持原密码
- **书源列表「配置消失」竞态修复**
- **书源数据损坏容错** — 跳过坏条目而非整表清空
- **OPDS Basic 认证**
- **明文 http 源开关**
- **下载也带认证** — 不会「能浏览、下不了」
- **中文错误提示与错误分类** — 401/403/404/5xx/超时/TLS/网络/非 OPDS
- **临时故障自动重试** — 仅 429/5xx，500ms→1s→2s 退避
- **目录请求超时 15s→30s** — 修「第一次必失败」
- **OPDS-1（Atom）目录浏览**
- **OPDS-2（JSON）书源支持** — Komga/Stump/php-opds
- **服务器不给 content-type 也能识别 JSON 目录**
- **非规范书源容错** — 无命名空间 feed、不带 rel 的下载链接等
- **搜索目录（OpenSearch）** — 支持 `{searchTerms}` 变量替换
- **简介 HTML 清洗** — 不再显示满屏标签
- **「残条」归类修复** — 只有封面的条目不再当文件夹
- **书目详情补全入口**
- **OPDS 分面（facet）解析**
- **下载格式与优先级** — epub > pdf > mobi > azw3 > txt
- **书源地址清洗** — 剥离控制字符、自动补 `https://`

**内置书源（本机服务端 + LibGen / Z-Library 驱动）**
- **内置书源服务端** — 把上游站点包装成本机 OPDS 目录
- **两端上线方式不同** — 安卓真起 127.0.0.1 服务；Windows 进程内短路（绕开 Clash/TUN 拦回环）
- **LibGen 内置源** — 3 个镜像自动轮换
- **LibGen 两步下载链 + 文件魔数校验**
- **LibGen 搜索结果字段完整**
- **Z-Library 内置源（账号密码登录）**
- **Z-Library 专属链接快路径**
- **Z-Library 驱动配置的保存与回显**
- **多镜像自动发现与自动换家** — 12 个种子域 + 官方清单刷新
- **上次可用镜像记忆（冷启动直达）** — 跳过十几秒多域探测
- **被验证页拦截时只识别、不绕过**
- **镜像全挂时的可读报错**
- **Z-Library 打开即「最热」列表**，排序/语言/格式可在搜索前先设好
- **最热列表 60 秒缓存**
- **Z-Library 搜索筛选** — 排序 8 / 语言 191 / 格式 12
- **相似书籍**
- **书籍评论**
- **下载额度用尽提示明确**
- **会话失效自动重登**
- **大文件下载不吃 15 秒超时**（放宽到 5 分钟）
- **下载内容兜底校验**
- **搜完还能原地再搜**
- **标准 OpenSearch 入口**（`opensearch.xml`）
- **未知驱动/非法请求有明确响应**
- **Z-Library 驱动自检（开发者）**

**检索 RAG**
- **向量检索不再卡死整轮搜索**（15 秒预算）
- **混合检索真正并行** — 向量与 BM25 同时开跑
- **向量结果被阈值过滤光时自动回退内存扫描**
- **桌面向量检索相似度阈值修正** — L2 距离换算成余弦
- **非法检索模式不再崩溃**
- **远程 embedding 限流自动重试** — 3 次递增退避

**翻译**
- **翻译模型按用途分别指定** — 取词(selectionModel) 与查词(dictionaryModel) 各配各的
- **未配置对应模型时直接报错**而非静默回落全局
- **词典查词提示词可自定义** — `{word}`/`{sourceLang}`/`{targetLang}` 占位符
- **翻译缓存按模式隔离**
- **新增单条文本缓存清除能力**
- **AI 翻译请求支持自定义参数** — system prompt / temperature / top_p 等
- **翻译请求改走 streaming fetch 且批量分支加 30 秒超时**

**设置与其他**
- **新增「开发者选项」设置分类**
- **新增阅读设置默认项** — 平滑翻页动画/划词即翻译/显示高亮色/标题栏可见
- **AI 防剧透改为分场景** — 旧布尔值自动迁移
- **聊天会话可一键清空全部**
- **翻译引擎新增 `external` 类型槽位**
- **引文可点击跳转定位** — `AttachedQuote`/`QuotePart` 新增 `cfi`
- **书籍新增「纯本地、不自动上传云端」标记**
- **`books.progress` / `currentCfi` 标记为废弃**
- **OPDS 整套中文文案（约 50 条）+ 各语种文案补齐**

### 开发与构建

- **expo prebuild 产物整目录入库** — `android/` 全量提交防漂移
- **Gradle wrapper 走腾讯云镜像**
- **Maven 仓库阿里云镜像优先**
- **ReactAndroid 改走源码复合构建** — `includeBuild` + `dependencySubstitution`，mavenLocal 已移除
- **gradle.properties 关键开关** — arm64-v8a / newArch / hermes / edgeToEdge / minify
- **debug 变体独立包名 `.debug`**
- **AndroidManifest 定制项** — queries / cleartext / singleTask / portrait / adjustResize 等
- **原生包注册** — Onnxruntime / ReaderSystemBars / ReaderKeyboardInsets
- **proguard 保留规则** — reanimated / turbomodule
- **Metro 配置加「禁止覆盖 serverRoot」告诫注释**
- **新增开发脚本 `scripts/restart-dev.sh`** — 一键重建 Windows 开发回路
- **`patches/react-native-readany.patch`** — Windows 禁用 LTO、Hermes 离线、ReactEditText 拖动分离
- **`patches/react-native-static-server-windows-cmake.patch`** — lighttpd Windows/Android CMake 修复
- **`patches/react-native/hermes-src/hermes.tar.gz`** — 15.6MB 离线 Hermes 源码备份
- **cli 测试日志 truncate 3GB → 64MB**（Windows 上会真实吃 3GB C 盘）
- **foliate-js `paginator.js` 选区与分页守卫**（122 行改动）

## 使用
- 查看完整改动:`local-mods` 分支(本说明所在分支)
- 已发布版本:
  - **v0.3.0**(2026-09-17)— 安卓 APK + Windows 安装包(NSIS `.exe` 推荐,另有 `.msi`)
  - **v0.2.0**(2026-09-07)— 安卓 APK
  - **v0.1.0**(2026-08-25)— 首次上传
